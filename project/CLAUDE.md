<!-- BEGIN: claude-universal managed block (do not edit between these markers — rerun installer to update) -->
# Project context

Universal rules live in `~/.claude/CLAUDE.md` + `~/.claude/docs/`. This file holds **only** what differs in this project. Delete sections you don't need; don't leave empty TODOs.

## Overrides (optional)

_Fill in only if the universal defaults don't fit:_

- Commit/branch convention if not `<type>: <description>`:
- Destructive commands that are pre-approved here:
- Project-specific MCP/skill/CLI to prefer:
- Files that must stay in sync (code ↔ doc mapping):

## Context (optional)

_Fill in only if Claude would otherwise guess wrong:_

- What this is:
- Stack highlights / version pins:
- `Run it locally`:
<!-- END: claude-universal managed block -->
